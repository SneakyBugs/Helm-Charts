# Default values for telemetry-system.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""

argocdNamespace: argocd
destinationCluster: cluster
destinationClusterDomain: cluster.local

kubePrometheusStack:
  chartVersion: 62.1.0
  grafana:
    adminUser: admin
    adminPassword: admin
    host: grafana.infra.sneakybugs.com

tempo:
  chartVersion: 1.9.0

loki:
  chartVersion: 6.10.0
